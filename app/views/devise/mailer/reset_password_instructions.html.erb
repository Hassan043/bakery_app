<style>
  /* Password Reset Email Styling */
  .email-container {
    font-family: sans-serif;
    color: #5C3D2E;
    background-color: #F8F4E9;
    padding: 2rem;
    border: 1px solid #D4B483;
    border-radius: 0.5rem;
    max-width: 28rem;
    margin: 0 auto;
  }

  .email-container p {
    margin-bottom: 1rem;
    line-height: 1.5;
  }

  .email-container p.greeting {
    font-size: 1.25rem;
    font-weight: bold;
    margin-bottom: 1.5rem;
  }

  .email-container a.reset-link {
    display: inline-block;
    background-color: #5C3D2E;
    color: #FFFFFF !important;
    font-weight: bold;
    padding: 0.5rem 1rem;
    text-decoration: none;
    border-radius: 0.5rem;
    transition: background-color 0.2s ease-in-out;
    margin-bottom: 1rem;
  }

  .email-container a.reset-link:hover {
    background-color: #3A2D1E;
  }

  .email-container p.notice {
    background-color: #E7D7C5;
    padding: 1rem;
    border-radius: 0.5rem;
    border: 1px solid #D4B483;
  }
</style>

<div class="email-container">
  <p class="greeting">Hello <%= @resource.email %>!</p>

  <p>Someone has requested a link to change your password. You can do this through the link below.</p>

  <p>
    <%= link_to 'Change my password',
                edit_password_url(@resource, reset_password_token: @token),
                class: 'reset-link' %>
  </p>

  <p>If you didn't request this, please ignore this email.</p>
  <p class="notice">Your password won't change until you access the link above and create a new one.</p>
</div>
