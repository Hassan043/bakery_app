<!DOCTYPE html>
<html>
  <head>
    <title>BakeryApp</title>    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="min-h-screen bg-gray-50 text-gray-800">
    <header style="background-color: #C2B280;" class="shadow-md">
      <div class="container mx-auto px-8 py-6 flex justify-between items-center">      
        <h1 class="text-2xl font-bold text-[#6E260E] flex items-center gap-2">
          <%= link_to root_path, class: "flex items-center gap-2" do %>
            <%= image_tag "/logo.png", alt: "Logo", class: "h-8 w-8" %>
            BakeryApp
          <% end %>
        </h1>


        <nav class="space-x-4">
          <%= link_to "Products", products_path, class: "hover:text-[#6E260E]" %>
          <%= link_to "Categories", categories_path, class: "hover:text-[#6E260E]" %>
          <%= link_to "Cart (#{session[:cart]&.size || 0})", cart_path, class: "hover:text-[#6E260E]" %>
          <% if user_signed_in? %>
            <%= link_to current_user.email, customer_dashboard_path, class: "hover:text-[#6E260E]" %>
            <%= button_to "Logout", destroy_user_session_path, method: :delete, class: "hover:text-red-500 inline-block" %>
          <% else %>
            <%= link_to "Login / Sign Up", new_user_session_path, class: "hover:text-[#6E260E]" %>
          <% end %>
        </nav>
      </div>
    </header>

    <main class="container mx-auto px-4 py-8">
      <% flash.each do |type, msg| %>
        <div class="mb-4 px-4 py-2 rounded 
                    <%= type == "notice" ? "bg-green-100 text-green-800" : "bg-red-100 text-red-800" %>">
          <%= msg %>
        </div>
      <% end %>

      <%= yield %>
    </main>

    <footer class="bg-white border-t mt-12">
      <div class="container mx-auto px-4 py-6 text-center text-gray-500">
        © <%= Time.current.year %> BakeryApp — Made with 🧁
      </div>
    </footer>
  </body>
</html>
